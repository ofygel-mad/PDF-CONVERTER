import { AccountClient } from "@/components/bbc-dashboard/access/account-client";

export const metadata = {
  title: "BBC Dashboard — личный кабинет",
};

export default function BbcAccountPage() {
  return <AccountClient />;
}
