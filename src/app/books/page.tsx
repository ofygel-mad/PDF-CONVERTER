import { redirect } from "next/navigation";

/**
 * `/books` больше не отдельная страница.
 *
 * Она была третьей поверхностью над теми же данными: страница с колонками и
 * гридом, раздел «Реестры» в сайдбаре с формой, и плитка в «Сервисах», ведущая
 * сюда. Один объект под тремя названиями и без единого места, где видно, что
 * это одно и то же.
 *
 * Всё съехалось в раздел «Книги» сайдбара дашборда. Ссылка оставлена живой,
 * чтобы сохранённый адрес приводил в приложение, а не в 404: разделы дашборда
 * живут в состоянии, а не в URL, поэтому точнее, чем «на дашборд», отправить
 * некуда.
 */
export default function BooksPage() {
  redirect("/bbc-dashboard");
}
