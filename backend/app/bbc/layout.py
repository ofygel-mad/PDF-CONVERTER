"""Привязка колонок по названиям — переехала в `app.books.layout`.

Машинерия оказалась нужна обеим сторонам. Раздел «Книги» предлагает привязку
поля к роли ровно теми же тремя ступенями — точное совпадение заголовка,
сжатое, похожее — и с тем же отказом, когда похожих кандидатов на денежную
колонку сразу несколько. А импортировать `app.bbc` он не имеет права: модуль,
задуманный общим, не должен знать про конкретную компанию.

Копия разъехалась бы с оригиналом на первой правке, причём молча: две функции с
одним именем, по-разному решающие, какая колонка наша.

Этот файл оставлен реэкспортом: `dataset.py`, `journal.py`, `sales.py` и
`tests/test_bbc_layout.py` продолжают импортировать отсюда и переезда не
заметили. Новый код берёт из `app.books.layout` напрямую.
"""
from __future__ import annotations

from app.books.layout import (
    Column,
    Drift,
    Layout,
    LayoutError,
    Placement,
    norm,
    resolve_layout,
    squash,
)

__all__ = [
    "Column",
    "Drift",
    "Layout",
    "LayoutError",
    "Placement",
    "norm",
    "resolve_layout",
    "squash",
]
